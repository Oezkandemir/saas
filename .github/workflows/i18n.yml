name: "Disabled i18n Translations"
# This workflow has been disabled due to permission issues
# The original workflow attempted to update translations but failed with 403 errors

on:
  # This workflow will never run
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Do nothing
        run: echo "This workflow is disabled due to permission issues"
